apiVersion: v1
kind: Secret
metadata:
  name: blast-api
  namespace: obol
  labels:
    app.kubernetes.io/name: blast-proxy
    app.kubernetes.io/part-of: obol-demo
type: Opaque
stringData:
  # 🔒 Replace with your actual values **before** applying,
  # or skip this file and create via:
  #   kubectl -n obol create secret generic blast-api \
  #     --from-literal=projectId='<YOUR_BLAST_PROJECT_ID>' \
  #     --from-literal=apiKey='<YOUR_X_API_KEY>'
  projectId: "<YOUR_BLAST_PROJECT_ID>"
  apiKey: "<YOUR_X_API_KEY>"

